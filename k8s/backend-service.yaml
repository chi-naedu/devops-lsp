apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP      
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80          # Service listens on 80
      targetPort: 5000  # Container listens on 5000